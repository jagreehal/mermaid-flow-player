---
title: Mermaid Flow Player
description: Step through your Mermaid diagrams. One script tag.
template: splash
hero:
  title: ' '
  tagline: ' '
---

import FlowPlayerElement from '~/components/FlowPlayerElement.astro';
import HeroPlayground from '~/components/HeroPlayground.astro';
import HomeStyles from '~/components/HomeStyles.astro';

<HomeStyles />

<div class="home">
  <header class="home__hero">
    <p class="home__eyebrow">
      <span class="home__eyebrow-dot" aria-hidden="true"></span>
      <span>Mermaid Flow Player</span>
    </p>

    <h1 class="home__title">
      Step through your <em class="home__title-em">Mermaid</em> diagrams.
    </h1>

    <p class="home__lede">
      Add one script tag. Every diagram on the page gets controls, and every
      step gets a URL you can send someone.
    </p>
  </header>

  <section class="home__stage" aria-label="Try it">
    <HeroPlayground />
  </section>

  <section class="home__jobs" aria-label="What people use it for">
    <div class="home__jobs-head">
      <h2 class="home__jobs-title">Three things people do with it</h2>
    </div>

    <article class="home__job">
      <div class="home__job-text">
        <h3>Explain a system to someone new</h3>
        <p>
          A new engineer reads an architecture diagram as a wall of boxes. Play
          it and they follow one request through the whole thing, in the order
          it actually happens.
        </p>
        <p class="home__job-meta">Narration comes from <code>%% narrate</code> lines in the diagram source.</p>
      </div>
      <div class="home__job-demo">
        <FlowPlayerElement
          controls
          narration
          diagram={`flowchart LR
  A[Browser] --> B[Cache]
  B -->|miss| C[Gateway]
  C --> D[Auth]
  D --> E[Orders]
  E --> F[(Postgres)]
  E --> G[Search]
  %% narrate A: A request starts in the browser.
  %% narrate B: The edge cache answers most of them without going further.
  %% narrate C: A miss goes to the gateway, which is where auth happens.
  %% narrate D: The token is checked once, here, not in every service.
  %% narrate E: Only now does the orders service see the request.
  %% narrate F: It reads from Postgres, the source of truth.
  %% narrate G: And writes to the search index so the next lookup is fast.`}
        />
      </div>
    </article>

    <article class="home__job">
      <div class="home__job-text">
        <h3>Walk an audience through a flow</h3>
        <p>
          You are on stage, or on a recorded call, and you need the room looking
          at the same arrow you are. Step forward when you are ready, not when
          the animation decides.
        </p>
        <p class="home__job-meta">Arrow keys and space work. So does linking to step 4.</p>
      </div>
      <div class="home__job-demo">
        <FlowPlayerElement
          controls
          narration
          diagram={`sequenceDiagram
  participant U as User
  participant A as App
  participant S as Auth
  participant D as DB
  U->>A: Submit form
  A->>S: Verify token
  S-->>A: Expired
  A->>S: Refresh
  S-->>A: New token
  A->>D: Write record
  D-->>A: Committed
  A-->>U: Saved`}
        />
      </div>
    </article>

    <article class="home__job">
      <div class="home__job-text">
        <h3>Upgrade a docs site you already have</h3>
        <p>
          Your diagrams are already written. Add the script tag and leave the
          markup alone. Auto mode finds hand-written <code>.mermaid</code>{' '}
          blocks and the fences your markdown pipeline renders, in both of the
          spellings that exist in the wild.
        </p>
        <p class="home__job-meta">
          Works with Docusaurus, VitePress, Astro, Starlight, Jekyll, Hugo,
          Eleventy, and anything built on markdown-it, marked, Prism or Shiki.
        </p>
      </div>
      <div class="home__job-demo">
        <FlowPlayerElement
          controls
          narration
          diagram={`flowchart TD
  A[Your markdown] --> B[Your build]
  B --> C[HTML with a mermaid block]
  C --> D[One script tag]
  D --> E[A player]`}
        />
      </div>
    </article>
  </section>

  <section class="home__more" aria-label="Read on">
    <a class="home__btn home__btn--primary" href="installation/">
      Every option
      <svg viewBox="0 0 16 16" width="14" height="14" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="1.6">
        <path d="M4 8h8M9 4l4 4-4 4" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </a>
    <a class="home__btn home__btn--ghost" href="features/showcase/">Showcase</a>
    <a class="home__btn home__btn--quiet" href="api/reference/">API reference</a>
  </section>
</div>
