{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "~/components": ["./src/components"],
      "~/components/*": ["./src/components/*"]
    }
  },
  "include": [".astro/types.d.ts", "**/*"],
  "//": "local-dist is the symlinked player build (pnpm link-player) and playwright-report is generated HTML — type-checking either makes `pnpm build` fail locally with dozens of errors from bundled code.",
  "exclude": ["dist", "public/local-dist", "playwright-report", "test-results"]
}
